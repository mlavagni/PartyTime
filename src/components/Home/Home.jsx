import React from "react";

function Home() {
  // map here
  // {const scoreUi = student.scores.map((score, idx) => (
  //   <Score scores={score} />
  //   ))}

  return <div>hello Home</div>;
}

export default Home;
